# FIXLOG 2026-09-22 — 登录态误报修复（基于实测页面特征）

## 事故现象

**用户收到告警**：「猎聘A 登录态失效」，但用户实机核查：
- 猎聘A **在线中**，浏览器页面正开着
- 反而是**猎聘B** 的浏览器页面没开着

→ 判定：**误报**。

## 核实过程与证据

| 证据 | 结果 |
|------|------|
| 浏览器实例（端口 9222）| 猎聘页面正在运行 ✓ |
| profile 活动时间 `/tmp/auto-apply/liepin-a` | 17:15（最新）|
| 今日统计 | 猎聘A 跳过 2290 个，几乎全是 `skip_chatted` |
| **"能识别到继续聊按钮"** | **本身即证明已登录**（未登录渲染不出该状态）|

**端口/账号实际对应**（排查中发现）：

| 端口 | 配置账号 | 实际状态 |
|------|---------|---------|
| 9222 | liepin-a | ✅ 猎聘页面在跑 |
| 9223 | boss-a | ✅ BOSS直聘在跑 |
| **9224** | **liepin-b** | ❌ **无服务（浏览器未启动）** |
| 9225 | boss-b | ✅ BOSS直聘在跑 |

→ 用户观察的"猎聘B 没开着"**属实**，但那不是告警所说的账号。

## 实测对比（按用户要求的方法论）

分别抓取两个平台各一组对照页面：

| 平台 | 已登录样本 | 未登录样本 |
|------|-----------|-----------|
| 猎聘 | 9222（猎聘A 运行中）| 9226（全新 profile `/tmp/liepin-loggedout`）|
| Boss | 9223（BossA 运行中）| 9227（全新 profile `/tmp/boss-loggedout`）|

### 差异表

**猎聘**

| 特征 | 已登录 | 未登录 | 区分度 |
|------|--------|--------|--------|
| cookie `liepin_login_valid` | 有 | 无 | ⭐⭐⭐ |
| cookie `user_name` / `user_photo` / `user_roles` | 有 | 无 | ⭐⭐⭐ |
| cookie `inited_user` / `imId_0` | 有 | 无 | ⭐⭐ |
| 头部文案 | "...我要招人 你好， 冯先生" | "...我是猎头 我是招聘方 NEW 登录/注册" | ⭐⭐⭐ |
| 按钮"登 录" | 无 | 有 | ⭐⭐⭐ |
| **职位卡片数** | **42** | **42** | ❌ **零区分度** |

**Boss直聘**

| 特征 | 已登录 | 未登录 | 区分度 |
|------|--------|--------|--------|
| DOM `[class*=logout]`（"退出登录"）| 有（1）| 无 | ⭐⭐⭐ |
| 头部文案 | "...消息 3 简历 Bruce..." | "...我要招聘 我要找工作 登录/注册 添加求职期望" | ⭐⭐⭐ |
| 职位卡片数 | 300 | 15 | ⭐⭐（不稳定）|
| `chat_btn` 数量 | 10 | 0 | ⭐⭐ |

## 根因

**两层错误叠加**：

1. **触发条件错误**：v1 用 `if (!finalCards)` 触发登录检测，假设"卡片数为 0 = 登录失效"。
   实测否定：**未登录也能看到 42 张卡片**（两个状态完全一致）。真正的触发场景其实是
   页面**加载中**卡片暂时为 0。
2. **判定特征太宽松**：v1 用 `正文含"登录"字样` 判未登录。猎聘页面**即使已登录**，
   页脚/推荐位也常态带"登录"字样 → 命中 → 误判。

两相叠加 → 页面加载时的一次瞬时 `finalCards=0` + 正文里的"登录"字样 = 误报。

## 修复（v2）

### 判定逻辑（`_diagnoseLogin`）

```
优先级：
1. URL 命中 /login|passport|signin          → logged_out（strong）
2. 已登录特征（任一）：
   - cookie liepin_login_valid / user_name / user_photo   (猎聘)
   - DOM [class*=logout] "退出登录"                        (Boss)
   - 头部文案 "你好，XX" / "退出登录"
3. 未登录特征：
   - 头部文案 "登录/注册" / "立即登录" / "扫码登录" 等
4. 判定：
   - 只有已登录特征 → logged_in  (strong)
   - 只有未登录特征 → logged_out (strong)
   - 两者都有      → uncertain   (不告警)
   - 两者都无      → unknown     (不告警)
```

**关键设计**：`uncertain` / `unknown` **一律不触发告警** —— 宁可漏报（由"本轮 0 投递"行为告警兜底），绝不误报。

### 结构性改动

| 改动 | 说明 |
|------|------|
| 登录检测**独立于卡片数** | 不再用 `if (!finalCards)` 当触发器（该前提已被实测否定）|
| `_confirmLoggedOut()` | 连测两次（间隔 5s）确认，防页面加载中的瞬时抖动 |
| 确认未登录后 `return` | 直接跳过该账号，不空跑浪费调度时间 |
| 分平台特征 | 猎聘靠 cookie，Boss 靠 DOM 元素，不假设通用 |

### 验证结果（回归测试 4/4 通过）

| 样本 | 期望 | 实际 | 依据 |
|------|------|------|------|
| 9226 猎聘未登录 | logged_out | ✅ | 文案(登录/注册) |
| 9227 Boss未登录 | logged_out | ✅ | 文案(登录/注册) |
| 9222 猎聘已登录 | logged_in | ✅ | cookie(liepin_login_valid/user_name) + 文案(你好，XX) |
| 9223 Boss已登录 | logged_in | ✅ | 元素(退出登录) |

## 新增工具

| 文件 | 用途 |
|------|------|
| `tools/login-fingerprint.js` | 特征采集器：连 CDP 端口，dump URL/title/cookie 名/头部文本/关键选择器。用法 `node tools/login-fingerprint.js <port>` |
| `tools/test-login-detect.js` | 回归测试：对已知状态的端口跑判定。用法 `node tools/test-login-detect.js [9222,9226,...]` |

**造未登录对照样本的标准做法**：
```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --remote-debugging-port=9226 --user-data-dir=/tmp/liepin-loggedout \
  --no-first-run --no-default-browser-check "<列表页 URL>" &
```

## 方法论留存

已将「**依据页面特征实测对比来判断页面状态**」写入 skill `spa-automation-methodology`（v2.1.0）：
- 新增章节「页面状态识别：必须实测对比，禁止凭猜（登录态/权限/限流/空池）」
- 陷阱表新增第 39、40 条
- 顺带修复了该 skill 的 frontmatter YAML 错误（`description` 引号跨行未闭合）

## 遗留问题（下一步）

1. **猎聘B 浏览器未启动**（端口 9224 无服务）—— 用户指定为第 3 优先级，暂不处理
2. **职位池投穿研究**（用户指定第 2 优先级）—— 需分平台研究识别方案：
   - 猎聘：`skip_chatted`（平台侧"继续聊"）vs `skip_processed`（本地去重）需拆开统计
   - Boss：沟通率高（~100%），需看是否已达平台上限
3. **猎聘A 自 9/8 起 0 投递** —— 独立问题（疑 `liepin.js:657` 的 `card_<索引>` 兜底 ID 污染 `_processedIds`），
   待新上线的 `reason` 字段积累一轮数据后可证实/证伪
4. **账号名乱码**（看板侧 8 个变体）—— 配置源头是干净的，乱码发生在上报 NAS 环节，未修
